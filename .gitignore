# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# ARES artifacts (regenerated by training; keep the dirs via .gitkeep)
data/*.txt
checkpoints/*.pt
checkpoints/*.pkl

# Keep loss curves out of git by default except the smoke-run curve referenced
# in the README (force-add that one with `git add -f plots/loss_curve.png`).
plots/*.png
